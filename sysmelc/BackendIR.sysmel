namespace: SysmelC definition: {
Class: BackendModule withFields: #{
    globals: OrderedCollection.

}.

Class: BackendFunction withFields: #{
    instructions: OrderedCollection.
}.

BackendModule
    withSelector: #initialize addMethod: {
        |$(BackendModule)self :: Void|
        self globals: OrderedCollection()
    }.

BackendModule
    withSelector: #compileFunction addMethod: {
        |$(BackendModule)self :: Void|
        self globals: OrderedCollection()
    }.
}.
